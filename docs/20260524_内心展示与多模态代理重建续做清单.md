# 2026-05-24 内心展示与多模态代理重建续做清单

## 当前目标

- [x] 前端把“真实刺激”和“内心代理”基本分开，按钮已拆成“播放代理音频 / 播放刺激原音 / 单对象试听”
- [x] 听觉内心展示改成可点选对象云，而不是单纯条柱
- [x] 听觉代理播放从简单蜂鸣改成基于频带/主频/能量包络的结构化重组
- [x] 视觉内心展示继续保持轮廓主导，不回退到稀疏方块感
- [ ] 检查文本顺序/后继优势的现状，确认是否还需要补强
- [x] 做一次浏览器/API 真机回归，确认新 run 已被前端吃到，内心展示主卡摘要与音频对象云已更新
- [x] 跑针对性测试，避免前端和感受器回归
- [ ] 再看一轮性能，优先找最重的实际热路径

## 当前判断

- 视觉主卡的“苹果轮廓不像原图”问题，主要是旧 run 和旧前端展示逻辑混淆过；新 run 的 contour bundle 已经能画出较清晰苹果轮廓。
- 听觉主失真源已经确认并修复：旧链路里大量 `0Hz / signal_presence=0` 的空窗因为新异性和焦点 bonus 被排到前面，导致对象云、摘要和代理播放都像杂音。现在已经补上 `signal_presence / signal_gate`，并让排序、摘要、前端展示都优先真实有结构的窗口。
- 记忆向量并不是完全没有内容特征，但视觉/听觉描述子目前仍偏离散化，后续可继续加厚。

## 2026-05-24 本轮已验证结果

1. 视觉
   - 新 run 在前端“内心展示”主卡上已经稳定显示：
     - `来源: 当前感知 / 近 4 tick / 最近视觉 tick#67`
     - `能量: 结构 2 / 动态 2 / 记忆 24`
     - `视觉 主帧=67 / 结构=2 / 动态=2 / 注视累积=731 / 画布=256x256`
   - 说明主卡已经不再错误落到最后一个空 tick，而是会自动回看最近真正有视觉内容的 tick。

2. 听觉
   - `tests/test_multimodal_sensors_phase11_12.py` 通过。
   - 新展示 run：`phase11_multimodal_run_20260524_054350_175`
   - 对这条新 run 的 API 抽查显示，tick 68~73 的听觉主窗口已基本变成有结构的真实窗口：
     - `dominant_hz` 稳定在约 `170Hz`
     - `feature_summary.signal_presence` 约 `0.91 ~ 0.93`
     - top windows 大多为 `signal_presence > 0.5` 的结构窗
   - 旧的“前几名几乎全是 `0Hz / signal_presence=0` 空窗”的问题已明显缓解。

3. 前端
   - 浏览器 DOM 已确认最新 run 出现在列表和摘要区。
   - 听觉热点文本已从旧 run 的坏窗，切换成新 run 的 `audio::win_5 / win_4 / win_7 / win_9` 一批结构窗。
   - `innerAudioStatus` 已能显示：
     - `低/中/高频 = 0.69 / 0.2 / 0`
     - `主频 = 172Hz / 窗级对象=16`

## 仍未完全理想的点

1. 听觉仍有轻微尾巴
   - 虽然主队列已明显变干净，但在部分 tick 的后几名里，仍会残留少量低信号窗或异常高频窗。
   - 这已经不是“完全听不清”的主问题，而是后续可继续做的精修项。

2. 内心展示画布的自动截图验证还不稳定
   - in-app browser 对这页的 `Page.captureScreenshot` 很容易超时。
   - 当前更可靠的验收方式仍是：
     - API 抽查
     - DOM 文本摘要
     - 运行侧导出的 contour bundle 图片

3. 文本顺序 / 后继优势 / 长程 replay 还没重新验收
   - 这部分仍是下一轮重点。

## 本轮优先级

1. 文本顺序 / 苹果-香蕉交替后继优势重新严谨验收
2. 多模态长程 300~1000 tick 场景化测试
3. 性能热路径复查，争取继续逼近 50ms/tick
4. 若需要演示图，再考虑单独做更轻的前端截图/导出通道

## 2026-05-24 本轮追加修复（展示副本 8767）

- [x] 确认 127.0.0.1:8767 不是主观测台常规服务，而是 outputs/_tmp_observatory_showcase_8767.py 拉起的展示副本进程。
- [x] 锁定关键问题：该展示副本在本轮排查时实际对前端返回的是 sset_ref/v1 形式的媒体占位，而不是已经恢复后的 data:image/... / base64 音频文本，导致前端主卡可能退回粗糙 fallback，看起来像“轮廓不对”“音频像杂音”。
- [x] 后端补充运行资产读取接口：GET /api/runs/<run_id>/assets/<rel_path>。
- [x] 前端新增 sset_ref 兼容解包：
  - isAssetRef(...)
  - esolveRunTextAsset(...)
  - 图片缓存、参考图、轮廓 bundle、代理音频、刺激原音 都可兼容旧格式 sidecar。
- [x] 重启 8767 展示副本进程后复查：
  - /api/health 已切到新 pid=31564
  - /api/runs/phase11_multimodal_run_20260524_054350_175/ticks/67/sidecar 中 preview_image.data_url 与 contour_reconstruction.composite_data_url 已恢复为真实 data:image/png;base64,... 字符串。
- [x] 新增回归覆盖：
  - 	ests/test_web_api.py 覆盖 run asset 读取接口
  - 	ests/test_observatory_frontend_phase17.py 覆盖前端 sset_ref 兼容锚点
- [x] 相关回归已通过：
  - pytest tests\test_observatory_frontend_phase17.py tests\test_web_api.py tests\test_multimodal_sensors_phase11_12.py tests\test_memory_store_v2.py -q
  - 结果：35 passed

## 当前新增判断

- 这次用户看到的“主轮廓不像原图”和“代理音频像杂音”，至少有一部分不是视觉/听觉感受器本体失效，而是展示副本服务把高保真媒体层降格成了未解包的 sset_ref，前端主卡因此没有真正用到 contour bundle 与代理音频数据。
- 视觉结构描述子仍然值得继续加厚，但展示链路错误已经被确认是一个独立且真实的主因，不能再把它误判成“单纯识别算法差”。
- 下一轮应该继续做两类验收：
  1. 不依赖 in-app browser 的稳定画布截图/导出验收
  2. 文本顺序 / 后继优势 / 长程多模态 replay / 性能热路径复查
