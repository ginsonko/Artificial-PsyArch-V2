# AP 二期 V2 行动模块与二维空间双坐标最终冷保存

日期：2026-05-20

## 1. 文档目的

这份文档用于把目前已经讨论收敛的两块设计固定下来：

1. **行动模块新版正式方案**
2. **二维图像空间双坐标系信息进入记忆与召回的正式方案**

本稿优先满足以下原则：

1. 不依赖大量硬编码的 if / else 和动作对动作冲突表
2. 保持 AP 哲学一致：统一竞争、统一记忆、统一预测、统一调制
3. 让“行动前预测后果”成为 AP 本体能力，而不是外部教师硬判
4. 让“犹豫”“习惯”“动作延续”“目标满足后停止继续乱做”都能自然出现

---

## 2. 这次最终口径的核心变化

### 2.1 不再采用“动作两两硬编码冲突强度表”

此前示例里出现过：

- 左看 vs 右看：高冲突
- 左看 vs 微扫左：低冲突
- 开左开关 vs 开右开关：高冲突

这个例子有助于解释直觉，但**不应成为正式实现方案**。  
原因是：

1. 真正系统里动作是带参的
2. 参数空间连续、组合爆炸
3. 若为每对动作硬定义冲突高低，会迅速不可维护

因此正式版改为：

> **同一行动器内部，只做统一的超阈值竞争与公共抑制，不做手写 pairwise 冲突表。**

也就是说，基础版本里：

1. 是否竞争，由 `actuator_id` 决定
2. 是否能同 tick 共存，由“每个行动器每 tick 只允许一个行动胜出”决定
3. “犹豫”来自**多个高于阈值的候选在同一行动器内彼此稀释后，没有任何一个留下足够高的超阈值余量**

这套机制天然支持带参动作，而不需要手工维护动作对。

### 2.2 不再把“选错压错、选对抬对”作为主机制

这种做法可以作为局部训练技巧，但不应成为主设计。  
正式设计应改为：

1. 当前现状中形成动作候选
2. 对每个动作候选做“如果执行它，会发生什么”的浅预测
3. 用预测结果调制动作驱动力
4. 在同行动器内做统一竞争
5. 执行胜者
6. 执行后真实反馈回写记忆

所以 AP 的动作塑形主链是：

```text
现状 -> 动作候选 -> 动作后果浅预测 -> 驱动力调制 -> 行动器竞争 -> 执行 -> 真实反馈 -> 记忆塑形
```

而不是：

```text
现状 -> 直接选动作 -> 外部再告诉它对不对
```

---

## 3. 行动模块最终设计总览

## 3.1 行动模块的基本哲学

正式固定为：

1. 每个动作候选都是一个**带参行动节点**
2. 每个带参行动节点都有一个当前驱动力
3. 带参行动节点按 `actuator_id` 分组到不同行动器
4. 每个行动器每个 tick 最多只执行一个胜出动作
5. 动作是否执行，不只取决于当前驱动力，还取决于：
   - 当前阈值
   - 当前疲劳
   - 当前是否犹豫
   - 对动作后果的预测
   - 当前目标是否已满足

这意味着：

### 行动节点不是“是否执行”的布尔值

它是一个连续值对象，拥有：

1. 驱动力
2. 参数
3. 来源
4. 预测后果
5. 习惯偏置
6. 半衰期

### 行动器不是一个纯粹的执行开关

它是一个小型的动态系统，维护：

1. 当前阈值
2. 疲劳
3. 切换代价
4. 驻留偏置
5. 当前活动程序
6. 最近执行历史

---

## 3.2 带参行动节点的标准数据结构

建议正式统一为：

```json
{
  "action_id": "action::move_gaze",
  "instance_id": "act_000123",
  "actuator_id": "actuator::vision_gaze",
  "action_family": "move_gaze",
  "params": {
    "x": 0.72,
    "y": 0.31
  },
  "goal_ids": ["goal::inspect_left_object"],
  "source_refs": ["bn::mem_0012", "cstar::bundle_0004"],
  "raw_drive": 0.63,
  "pred_modulation": 1.18,
  "habit_modulation": 1.06,
  "context_modulation": 0.97,
  "validity_modulation": 1.0,
  "satisfied_modulation": 1.0,
  "final_drive": 0.76,
  "half_life_ticks": 4,
  "meta": {
    "reason": "预测可获得更高信息增益"
  }
}
```

其中：

1. `raw_drive`
   - 动作在尚未考虑预测后果前的基础驱动力

2. `pred_modulation`
   - 对“执行该动作后的预测后果”的乘算调制

3. `habit_modulation`
   - 习惯、近期成功历史带来的轻量促通或抑制

4. `validity_modulation`
   - 当前场景下这个动作是否仍然合理

5. `satisfied_modulation`
   - 若目标已满足，则同目标的替代动作快速塌缩

---

## 4. 动作驱动力的数学模型

## 4.1 基础驱动力不建议简单线性加总

原因：

1. 多个小杂音会被线性堆高
2. 不利于控制上界
3. 不利于不同来源证据的融合

正式建议：

```text
B_a = 1 - Π_i (1 - w_i s_i)
```

其中：

1. `B_a`
   - 动作 `a` 的基础驱动力

2. `s_i ∈ [0, 1]`
   - 各来源证据强度

3. `w_i ∈ [0, 1]`
   - 对应来源权重

这实际上是一个饱和式证据融合结构，可理解为：

> 多个来源都在说“这个动作值得做”，但它们不会无限线性叠加。

### 建议纳入 `B_a` 的来源

1. 规则直接给出的驱动力
2. 动作对应节点在状态池中的虚能量增加量
3. 当前未满足目标强度
4. 习惯记忆热启动
5. 近期残留意图

### 建议初始权重

1. `w_rule = 0.55`
2. `w_virtual = 0.75`
3. `w_goal = 0.85`
4. `w_habit_seed = 0.35`
5. `w_residual = 0.45`

这不是永久常数，而是第一版推荐初值。

---

## 4.2 奖励、惩罚、期待、压力等应主要以乘算调制出现

这是本次最终明确固定的原则之一。

原因是：

1. 它们本质上是对“预测到的动作价值”的调制
2. 若把它们做成加算常量，容易让错误预测或弱噪音也持续抬高动作
3. 乘算更符合“在已有动机基础上放大或抑制”的哲学

正式定义：

```text
U_a =
  c_a * (
    1.00 * Δreward
  + 0.45 * Δexpectation
  + 0.35 * Δcorrectness
  - 1.15 * Δpunishment
  - 0.55 * Δpressure
  - 0.60 * Δdissonance
)
```

其中：

1. `Δreward`
   - 假设执行动作后，预测包中奖励信号的增加量

2. `Δpunishment`
   - 假设执行动作后，预测包中惩罚信号的增加量

3. `Δexpectation / Δpressure / Δcorrectness / Δdissonance`
   - 假设执行动作后，对应认知感受/情绪通道的预测变化量

4. `c_a ∈ [0,1]`
   - 对这次动作后果浅预测的置信度门控

然后定义：

```text
M_pred(a) = clip(exp(0.9 * U_a), 0.25, 2.2)
```

这意味着：

1. 若动作后果预测很好，`M_pred > 1`
2. 若动作后果预测很差，`M_pred < 1`
3. 但上界和下界都有限，防止极端爆炸

---

## 4.3 最终驱动力

建议正式统一为：

```text
D_a = B_a * M_pred(a) * M_habit(a) * M_context(a) * M_valid(a) * M_satisfied(a)
```

各乘子含义：

1. `M_habit`
   - 习惯促通或抑制
   - 推荐范围：`0.8 ~ 1.5`

2. `M_context`
   - 当前任务、慢情绪风格、长期目标对动作风格的调制
   - 推荐范围：`0.7 ~ 1.4`

3. `M_valid`
   - 当前前提是否成立
   - 若动作当前已明显无效，可降到 `0.05 ~ 0.2`

4. `M_satisfied`
   - 若动作服务的目标已被满足，则快速压低
   - 推荐范围：`0.05 ~ 0.1`

---

## 5. 动作后果浅预测层

## 5.1 为什么必须有这一层

这是动作系统从“反射式执行器”变成“可学习、可塑形行动者”的关键。

没有它时：

1. 动作只能依赖当前局部 drive
2. 奖惩要等执行后才知道
3. 系统缺乏“想一下如果我这么做会怎样”的能力

有它时：

1. 动作可以被未来后果调制
2. 奖励/惩罚不只是执行后修正，也能提前影响竞争
3. 习惯不是纯表格 bias，而是通过记忆召回到“以前类似情境下做过这个动作，后面如何”

## 5.2 实现原则

为了控制算力，动作后果预测不做全量展开，只做：

1. 每个行动器先选 `raw_drive` 前 `K_pre` 个候选
2. 对这 `K_pre` 个候选分别构造：

```text
当前现状 + 假设执行该动作
```

3. 对每个假设分支做浅层 recall
4. 读出该分支的预测奖惩与情绪变化
5. 形成 `M_pred(a)`

### 推荐配置

1. `K_pre = 2 ~ 4`
2. 动作后果浅预测深度：
   - 一级 recall 必做
   - 二级邻域 recall 可选，默认轻量

3. 强制缓存：
   - `context_signature + action_signature -> predicted outcome`

这样既保留“行动前预想后果”，又不把算力拉爆。

---

## 6. 行动器竞争与犹豫机制

## 6.1 不采用 pairwise 冲突表

正式版基础层面不再使用：

```text
conflict(a,b) = 手工定义高低
```

而是改成：

> **同一行动器中，所有高于阈值的候选共享一个竞争抑制场。**

这意味着：

1. 不需要为任意参数组合写冲突表
2. 只要两个动作属于同一行动器，它们就天然竞争
3. 谁更强，取决于当前驱动力与阈值余量

## 6.2 统一公共抑制机制

对某个行动器 `k`：

1. 当前阈值：`T_k`
2. 候选驱动力从高到低排序：
   - `D_1 >= D_2 >= ... >= D_n`

只看超过阈值的候选集合：

```text
A_k^+ = { a | D_a > T_k }
```

若：

1. `|A_k^+| = 0`
   - 本 tick 不执行

2. `|A_k^+| = 1`
   - 直接执行该动作

3. `|A_k^+| >= 2`
   - 进入统一竞争抑制

定义：

```text
I_k = max(0, D_2 - (T_k - ε))
```

然后对所有高于阈值的候选统一做：

```text
D'_a = D_a - I_k
```

这一步的含义是：

> 若第二名也高于阈值，则把整个行动器中所有超阈值候选一起往下压，直到第二名刚好掉到阈值以下。

这样会带来两个好处：

1. 自动保证同一行动器同一 tick 最多只剩一个候选明显高于阈值
2. 不需要知道“左看和右看冲突高不高”，只需要知道它们属于同一行动器

## 6.3 犹豫条件

统一抑制后，定义：

```text
margin_k = D'_1 - T_k
```

若：

```text
margin_k <= δ_hesitate
```

则判定为：

```text
hesitation_k = true
```

本 tick 该行动器不执行动作。

### 推荐参数

1. `ε = 0.01`
2. `δ_hesitate = 0.02 ~ 0.05`

这意味着：

1. 若两个动作非常接近，则会出现真实犹豫
2. 若第一名明显强于第二名，则仍会执行

## 6.4 “坚决程度”定义

动作执行的“坚决程度”正式定义为：

```text
firmness_a = max(0, D'_a - T_k)
```

再归一化成：

```text
firmness_norm = clip(firmness_a / F_scale, 0, 1.5)
```

推荐：

```text
F_scale = 0.25
```

这样：

1. 刚刚超过阈值的动作，执行较犹豫
2. 远高于阈值的动作，执行更坚决

---

## 7. 驱动力消耗、半衰期与动作延续

## 7.1 不再消耗整个驱动力，只消耗超阈值部分

正式固定为：

> **动作执行不消耗动作的全部驱动力，只消耗高于阈值的那一段余量。**

这样做的原因是：

1. 保留底层动机
2. 只消耗本轮用于推动动作出手的“坚决部分”
3. 更适合多 tick 连续动作

定义：

```text
E_a = max(0, D'_a - T_k)
```

执行后：

```text
D_after(a) = min(D'_a, T_k) + (1 - η_exec) * E_a
```

其中：

```text
η_exec ∈ [0, 1]
```

推荐初值：

```text
η_exec = 0.85
```

含义：

1. 执行后，大部分超阈值余量被消耗
2. 但阈值以下的基底倾向仍保留

## 7.2 未执行候选不做强惩罚，只做衰减

正式固定为：

1. 未胜出的动作，不视作“犯错”
2. 未胜出的动作，不直接清零
3. 未胜出的动作只按半衰期衰减

定义：

```text
D_next(a) = carry_ratio(a) * D_after(a) * exp(-ln(2) / τ_a) + new_input(a)
```

推荐：

### 犹豫未执行

```text
carry_ratio = 0.90
```

### 清晰落败但目标未满足

```text
carry_ratio = 0.65 ~ 0.75
```

### 目标已满足后的替代动作

```text
carry_ratio = 0.05 ~ 0.10
```

## 7.3 半衰期建议

### 视焦点瞬时动作

```text
τ = 2 tick
```

### 普通替代性操作动作

```text
τ = 4 ~ 6 tick
```

### 发言单词元级动作

```text
τ = 1 ~ 2 tick
```

### 工具性目标动作

```text
τ = 6 ~ 10 tick
```

### 长任务意图

```text
τ = 12 ~ 30 tick
```

---

## 8. 行动器阈值、疲劳与防抖

## 8.1 情绪主要作用在阈值层

这次正式固定为：

> 奖励/惩罚对单个动作主要通过乘算调制体现；  
> 情绪慢变量更适合主要作用在行动器阈值层。

定义：

```text
T_k =
  (T_base,k + F_k + H_k + SwitchCost_k)
  * exp(
      0.35 * Pressure_g
    + 0.25 * Dissonance_g
    - 0.20 * Expectation_g
    - 0.15 * Correctness_g
    )
```

其中：

1. `F_k`
   - 行动器疲劳

2. `H_k`
   - 犹豫/冲突后的短时抬阈

3. `SwitchCost_k`
   - 切换到新动作的代价

4. `Pressure_g / Dissonance_g / Expectation_g / Correctness_g`
   - 全局慢变量

这和“情绪通道是行动阈值调制器”的主哲学一致。

## 8.2 行动器疲劳更新

推荐：

```text
F_k(t+1) = 0.72 * F_k(t) + 0.22 * executed_k
```

其中：

1. 若本 tick 该行动器执行了动作，疲劳上升
2. 若没有执行，疲劳自然衰减

## 8.3 犹豫抬阈更新

推荐：

```text
H_k(t+1) = 0.55 * H_k(t) + 0.10 * hesitation_k
```

这意味着：

1. 刚刚犹豫过，下一 tick 更不容易被小波动拉着乱动
2. 但这个效应很短，不会长期卡死

## 8.4 推荐基础阈值

### 视焦点行动器

```text
T_base = 0.38
```

### 手部/点击/按键类

```text
T_base = 0.52
```

### 发言类

```text
T_base = 0.48
```

### 内部连续思考类

```text
T_base = 0.42
```

---

## 9. 目标满足后的主动塌缩机制

这是防止“它把所有替代动作轮流都做一遍”的关键。

正式固定为：

> **同一目标一旦已满足，其它替代动作的驱动力必须快速主动塌缩。**

### 示例

目标：

```text
goal::fan_on
```

候选动作：

1. 开左开关
2. 开右开关

若开左开关后，观测到风扇已转，则：

```text
M_satisfied(开右开关) -> 0.05 ~ 0.10
```

于是它不再继续执行右开关。

这一步不依赖硬编码“左开关和右开关冲突”，而依赖：

1. 它们共享 `goal_id`
2. 该 `goal_id` 已满足

---

## 10. 已尝试但无效后的主动抑制

若某动作刚执行过，但未达到目标，则需要对它加短时抑制，避免反复立即重试同一步。

定义：

```text
M_tried_no_effect(a) = 0.35
```

持续：

```text
2 ~ 4 tick
```

这会自然让：

1. 已尝试无效的动作短时变弱
2. 同目标的替代动作更容易胜出

### 风扇示例

1. 想开风扇
2. 先试左开关
3. 风扇没转
4. 左开关短时抑制
5. 右开关仍保留目标驱动力
6. 下一个 tick 右开关更可能胜出

---

## 11. 习惯模块正式定位

正式固定为：

> 习惯不是一张独立的大真理表，而是动作经验记忆的热缓存层。

它的来源应该主要是：

1. `现状 + 动作 + 后果` 经验链
2. 对这类经验链的 recall
3. 近期高命中经验形成的轻量缓存偏置

因此：

### 主机制

```text
相似现状 -> 召回以前做过的动作 -> 召回该动作后果 -> 调制当前 drive
```

### 缓存机制

`action_learning_v2` 可保留，但降格为：

1. 热缓存
2. 启动加速层
3. 近期偏好摘要层

而不应再承担完整“动作学习本体”的角色。

---

## 12. 二维图像空间双坐标系最终方案

## 12.1 正式固定：保留两套坐标

这是本次讨论后正式固定的空间方案。

视觉对象或 patch 必须同时保留：

### A. 稳定二维坐标

用于表示对象在图像/屏幕平面中的相对稳定位置。

建议字段：

1. `screen_x`
2. `screen_y`
3. `screen_w`
4. `screen_h`

值域统一归一化到 `[0, 1]`。

这套坐标用于：

1. 记忆写入
2. 邻域检索
3. 跨 tick 匹配
4. 空间召回

### B. 焦点相对坐标

用于表示对象相对于当前 `gaze_center` 的偏移。

建议字段：

1. `dx_from_gaze`
2. `dy_from_gaze`
3. `dr_from_gaze`

这套坐标用于：

1. 当前采样概率
2. 当前显著性
3. 视焦点行动驱动力
4. 是否值得继续跟踪

这两套坐标都必须保留。  
不允许只保留其一。

---

## 12.2 为什么必须双坐标共存

若只保留焦点相对坐标，会出现：

1. 焦点一移动，对象位置表达整体漂移
2. 同一对象跨 tick 难以稳定绑定
3. 不利于长期空间记忆

若只保留稳定坐标，会失去：

1. 当前对象相对视焦点的显著性
2. 是否值得立刻把 gaze 移过去
3. 视焦点候选动作的驱动力依据

所以正式原则是：

> **稳定坐标负责“对象在哪里”，相对坐标负责“我当前看它有多方便、值不值得去看”。**

---

## 12.3 视觉 patch / 对象的数据结构

建议统一为：

```json
{
  "sa_label": "vision::patch_3_5",
  "display_text": "视觉块[3,5]",
  "energy": 0.41,
  "coords": {
    "screen_x": 0.72,
    "screen_y": 0.31,
    "screen_w": 0.10,
    "screen_h": 0.12,
    "dx_from_gaze": 0.18,
    "dy_from_gaze": -0.07,
    "dr_from_gaze": 0.19
  },
  "attributes": {
    "brightness": 0.64,
    "motion": 0.21,
    "gaze_bonus": 0.78,
    "focus_affinity": 0.69
  }
}
```

若后续从 patch 级升级到对象级，结构不变，只替换：

1. `vision::patch_*`
2. 为 `vision::object::*`

---

## 12.4 记忆写入时的空间签名

当前仅取均值中心点还不够。  
正式建议升级为空间签名：

### 必保留项

1. `center_x`
2. `center_y`
3. `spread_x`
4. `spread_y`
5. `bbox_min_x`
6. `bbox_max_x`
7. `bbox_min_y`
8. `bbox_max_y`
9. `focus_dx_mean`
10. `focus_dy_mean`
11. `focus_dr_mean`
12. `space_source_count`

### 可选增强项

1. `occupied_quadrants`
2. `top_patch_positions`
3. `dominant_side`
4. `dominant_vertical_band`

这意味着记忆里的空间不再只是：

```text
一个点
```

而是：

```text
一个粗粒度空间分布签名
```

---

## 12.5 空间信息如何进入 embedding

正式建议在 memory embedding 中加入以下 token：

### 稳定坐标 token

1. `sp::abs_x::bucket`
2. `sp::abs_y::bucket`
3. `sp::spread_x::bucket`
4. `sp::spread_y::bucket`
5. `sp::bbox_x::bucket_pair`
6. `sp::bbox_y::bucket_pair`

### 焦点相对坐标 token

1. `sp::rel_x::bucket`
2. `sp::rel_y::bucket`
3. `sp::rel_r::bucket`

### 推荐量化

#### 绝对位置

`8 x 8` 或 `10 x 10` 桶

#### 相对位置

1. 左远
2. 左近
3. 中央
4. 右近
5. 右远

纵向同理。

重点不是特别精，而是：

1. 稳定
2. 可召回
3. 可跨 tick 对齐

---

## 12.6 query 侧也必须显式带空间 token

这是空间真正参与一级召回的关键。

如果只在记忆写入时保留空间，而 query 不带空间，那么：

1. 空间只能在后续邻域阶段起弱作用
2. 一级召回仍主要看标签和语义
3. 很难严格证明“按位置召回”

因此正式建议：

### query vector 额外加入

1. `q_abs_x::*`
2. `q_abs_y::*`
3. `q_rel_x::*`
4. `q_rel_y::*`
5. `q_rel_r::*`
6. `q_spread_x::*`
7. `q_spread_y::*`

这样就能形成：

> 同样是苹果，但左侧苹果与右侧苹果在一级召回上开始分化。

---

## 12.7 时空索引的职责重新固定

正式定义：

### 一级 recall

负责：

1. 当前现状的主匹配
2. 语义 + 空间 + 模态共同决定的第一层召回

### 二级 spacetime 邻域

负责：

1. 已命中记忆周边的时间延展
2. 已命中记忆周边的空间延展
3. 连续事件链补全

也就是说：

> 空间不应只靠二级邻域去挽救，而必须从一级 query 开始就参与竞争。

---

## 13. 动作与双坐标空间的耦合

这两块最终应该是同一套链上的。

## 13.1 视焦点行动的驱动力来源

`move_gaze_to(x, y)` 的驱动力应来自：

1. 稳定坐标下该目标的重要性
2. 焦点相对坐标下该目标的可达性
3. 当前信息增益预期
4. 该方向的历史经验预测

即：

```text
raw_drive(move_gaze_to target)
  <- object_salience
   + focus_relative_gain
   + info_gain
   + habit_seed
```

然后再由：

```text
predicted_reward / punishment / expectation / pressure
```

做乘算调制。

## 13.2 空间记忆会反过来塑形 gaze 行动

这意味着：

1. 过去某类视觉位置上经常有重要信息
2. 当前类似场景再次出现
3. 相关位置的 gaze 动作更容易获得驱动力

于是可形成：

1. 自动看向常出现关键目标的位置
2. 自动回看易出错区域
3. 自动注视高信息价值区域

这正是双坐标系统和行动模块耦合后的价值。

---

## 14. 观测与可审计字段

为了让后续实验可验收，观测台应至少增加或稳定显示：

## 14.1 行动模块

1. `actuator_id`
2. `T_k`
3. `F_k`
4. `H_k`
5. `SwitchCost_k`
6. 当前行动器 top 候选
7. 每个候选：
   - `raw_drive`
   - `pred_modulation`
   - `habit_modulation`
   - `satisfied_modulation`
   - `final_drive`
   - `post_inhibition_drive`
8. `I_k`
9. `hesitation_k`
10. `firmness`

## 14.2 双坐标空间

1. 当前 `gaze_center`
2. 当前 patch / object 的稳定坐标
3. 当前 patch / object 的相对坐标
4. 当前写入记忆的空间签名
5. recall 结果中的空间 token 命中
6. spacetime 邻域的 `space_bonus`

---

## 15. 最终推荐的落地方向

### 行动模块

1. 增加 `actuator_state` 在线旁路表
2. 将带参动作按 `actuator_id` 分桶
3. 增加动作后果浅预测
4. 用乘算调制替代奖励/惩罚对动作的主加算逻辑
5. 引入统一公共抑制，不引入 pairwise 冲突表
6. 引入犹豫判定
7. 执行时只消耗超阈值余量
8. 引入目标满足塌缩和无效尝试抑制

### 双坐标空间

1. 统一 patch / object 坐标字段
2. 升级 memory spacetime 签名
3. memory embedding 增加稳定坐标和相对坐标 token
4. query embedding 增加空间 token
5. 保留二级 spacetime 邻域层
6. 做“左/右、近/远、相对 gaze”三类实验

---

## 16. 一句话定稿

本次最终定稿口径是：

> AP 二期 V2 的动作系统，应当以“带参行动节点 + 行动后果浅预测 + 乘算调制 + 同行动器统一公共抑制 + 阈值/疲劳/犹豫控制 + 目标满足后主动塌缩”为正式设计；  
> AP 二期 V2 的二维视觉空间系统，应当以“稳定坐标 + 焦点相对坐标”双坐标并存，并同时进入记忆写入、一级 query、向量 embedding、时空邻域与观测审计链路。

这份文档优先级高于之前的零散聊天表述，后续若要工程落地，应以本稿为准。
