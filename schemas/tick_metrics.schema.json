{
  "type": "object",
  "required": [
    "schema_id",
    "schema_version",
    "run_id",
    "tick_index",
    "logic_ms",
    "live_payload_bytes",
    "state_top_count",
    "r_state_count",
    "a_focus_count"
  ],
  "properties": {
    "schema_id": { "type": "string", "enum": ["tick_metrics/v1"] },
    "schema_version": { "type": "string" },
    "run_id": { "type": "string" },
    "tick_index": { "type": "integer", "minimum": 0 },
    "logic_ms": { "type": "number", "minimum": 0 },
    "live_payload_bytes": { "type": "integer", "minimum": 0 },
    "state_top_count": { "type": "integer", "minimum": 0 },
    "r_state_count": { "type": "integer", "minimum": 0 },
    "a_focus_count": { "type": "integer", "minimum": 0 }
  },
  "additionalProperties": true
}

